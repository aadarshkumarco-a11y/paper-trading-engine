"""Multi-page modules for the NSE Paper Trading terminal.

Each page is a thin wrapper that composes ``ui.components`` widgets. Shared
state lives in ``ui.state`` so pages share a single ``TradingApp`` instance.
"""
